---
// import * as greatPost from './posts/great-post.md';
import Header from "./Header.astro";
const posts = Object.values(import.meta.glob('../pages/posts/*.md', { eager: true }));
---


<div>
  <!-- <p>{greatPost.frontmatter.title}</p> -->
  <!-- <p>Written by: {greatPost.frontmatter.author}</p> -->

  <Header></Header>

  <main>
    <!-- <h1>Posts:</h1> -->
    <!-- <section> -->
      {posts.map(post => <div class="post-preview">{post.frontmatter.date} - <a href={post.url}>{post.frontmatter.title}</a></div>)}
    <!-- </section> -->
  </main>
</div>

<style>
  main {
		padding: 15px 0px 0px 0px;
	}

  .post-preview {
    margin-bottom: 10px;
  }
</style>